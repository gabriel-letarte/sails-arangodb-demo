<script>
  var userLinks = [
    <% for(var i=0; i<neighbors.length; i++) { %>
        {source: '<%= user.username %>', target: '<%= neighbors[i].username %>', type: 'licensing'},
    <% } %>
  ];
</script>
